'use client';

/**
 * Dashboard Shell Kit
 * 
 * Re-exports the defaultKit from @hit/ui-kit.
 * The kit uses design tokens for consistent theming.
 */

export { defaultKit as erpKit } from '@hit/ui-kit';
